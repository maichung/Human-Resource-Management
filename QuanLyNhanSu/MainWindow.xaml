<Window x:Class="QuanLyNhanSu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyNhanSu"
        mc:Ignorable="d"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="YOLOHR" Height="750" Width="1300" WindowStartupLocation="CenterScreen" 
        Icon="Resources/Icons/logo.png" Foreground="#FF232C3A" FontSize="14"
        DataContext="{StaticResource MainVM}"
        Name="mainWindow">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding SetupQuyenCommand}">
            </i:InvokeCommandAction>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!-- Thanh menu -->
        <Grid Grid.Column="0" Background="#F7F8F9">
            <Grid.RowDefinitions>
                <RowDefinition Height="160"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <!-- Thông tin tài khoản -->
            <Grid Grid.Row="0">
                <StackPanel Margin="30 0 0 0" VerticalAlignment="Center">
                    <Ellipse Width="60" Height="60" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding AvatarSource}" Stretch="Uniform" />
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="{Binding TaiKhoanHienThi.NHANVIEN.HOTEN_NV}" HorizontalAlignment="Left" FontSize="16" 
                               FontFamily="Segoe UI Semibold" TextWrapping="Wrap"></TextBlock>
                    <TextBlock Text="{Binding TaiKhoanHienThi.NHANVIEN.CHUCVU_NV}" HorizontalAlignment="Left" FontSize="12" 
                               Foreground="#6F7A80" TextWrapping="Wrap"></TextBlock>
                    <Button Content="Đăng xuất" HorizontalAlignment="Left" 
                               Foreground="#6F7A80" FontSize="12" Background="{x:Null}" BorderBrush="{x:Null}" Margin="-15,0,0,0" FontWeight="Normal">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding DangXuatCommand}"
                                                       CommandParameter="{Binding ElementName=mainWindow}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <GridSplitter Height="1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" IsEnabled="False" Background="#FFD6D6D6"></GridSplitter>
            </Grid>

            <!-- Menu chính -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0 20 0 0">
                <!-- Trang chủ -->
                <StackPanel Name="btnTrangChu" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="0">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnTrangChu.png"/>
                    <Button Content="Trang chủ" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="0">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnTrangChuCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Nhân viên -->
                <StackPanel Name="btnNhanVien" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="1">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnNhanVien.png"/>
                    <Button Content="Nhân viên" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="1">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnNhanVienCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Phòng ban -->
                <StackPanel Name="btnPhongBan" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="2">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnPhongBan.png"/>
                    <Button Content="Phòng ban" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="2">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnPhongBanCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Nghỉ phép -->
                <StackPanel Name="btnNghiPhep" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="3">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnNghiPhep.png"/>
                    <Button Content="Nghỉ phép" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="3">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnNghiPhepCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Chấm công -->
                <StackPanel Name="btnChamCong" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="4">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnChamCong.png"/>
                    <Button Content="Chấm công" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="4">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnChamCongCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Lương -->
                <StackPanel Name="btnLuong" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="5">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnLuong.png"/>
                    <Button Content="Lương" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="5">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnLuongCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Tuyển dụng -->
                <StackPanel Name="btnTuyenDung" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="6">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnTuyenDung.png"/>
                    <Button Content="Tuyển dụng" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="6">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnTuyenDungCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Chi phí -->
                <StackPanel Name="btnChiPhi" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="7">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnChiPhi.png"/>
                    <Button Content="Chi phí" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="7">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnChiPhiCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Báo cáo -->
                <StackPanel Name="btnBaoCao" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="8">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnBaoCao.png"/>
                    <Button Content="Báo cáo" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="8">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnBaoCaoCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!-- Cài đặt -->
                <StackPanel Name="btnCaiDat" Orientation="Horizontal" Height="45" Margin="0 0 0 5">
                    <Grid Width="7" Margin="0">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Background" Value="{x:Null}"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="9">
                                        <Setter Property="Background" Value="#8062FE"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                    <Grid Width="25"></Grid>
                    <Image Stretch="None" Source="Resources\Icons\btnCaiDat.png"/>
                    <Button Content="Cài đặt" FontSize="16" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF232C3A" Margin="10,0,0,0" Height="Auto">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="9">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BtnCaiDatCommand}">
                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Nội dung -->
        <Grid Grid.Column="1" Margin="20 0 20 10">
            <!-- Trang chủ -->
            <Grid Name="gridTrangChu">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="0">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Trang chủ" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="36" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <ScrollViewer Grid.Row="1" FontSize="16" DataContext="{StaticResource MainVM}" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <WrapPanel Orientation="Horizontal">
                        <!-- Thẻ nghỉ phép -->
                        <materialDesign:Card Width="280" Height="250" Margin="5,0,30,35">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"></RowDefinition>
                                    <RowDefinition Height="5*"></RowDefinition>
                                    <RowDefinition Height="2*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <!-- Tiêu đề thẻ -->
                                <Grid Grid.Row="0">
                                    <Rectangle VerticalAlignment="Top" Height="3" Fill="#C28845"></Rectangle>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 10" >
                                        <TextBlock Text="Nghỉ phép" FontSize="18" Foreground="#FF404855" FontFamily="Segoe UI Semibold"></TextBlock>
                                        <Border  CornerRadius="8" Background="#C28845" Width="20" Height="20" Margin="5,0,0,0">
                                            <TextBlock Text="{Binding SoLuongNghiPhep}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="12"></TextBlock>
                                        </Border>
                                    </StackPanel>

                                    <Separator VerticalAlignment="Bottom" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                </Grid>

                                <!-- Nội dung thẻ -->
                                <Grid Grid.Row="1" Margin="10 0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition Height="4*"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <TextBlock Text="Hôm nay" Grid.Row="0" VerticalAlignment="Center" Foreground="#FF6F7A80" Margin="10,0,0,0"></TextBlock>
                                        </Grid>
                                        <ScrollViewer Grid.Row="1" Margin="0,5,0,0" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding ListNhanVienNP1}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>

                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Ellipse Width="35" Height="35" Margin="10" ToolTip="{Binding NhanVien.HOTEN_NV}">
                                                            <Ellipse.Fill>
                                                                <ImageBrush ImageSource="{Binding Avatar}" Stretch="Uniform" />
                                                            </Ellipse.Fill>
                                                        </Ellipse>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                    <Grid Grid.Column="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition Height="4*"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <TextBlock Grid.Row="0" VerticalAlignment="Center" Foreground="#FF6F7A80" Margin="10,0,0,0">7 ngày tới</TextBlock>
                                        </Grid>

                                        <ScrollViewer Grid.Row="1" Margin="0,5,0,0" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding ListNhanVienNP7}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>

                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Ellipse Width="35" Height="35" Margin="10" ToolTip="{Binding NhanVien.HOTEN_NV}">
                                                            <Ellipse.Fill>
                                                                <ImageBrush ImageSource="{Binding Avatar}" Stretch="Uniform" />
                                                            </Ellipse.Fill>
                                                        </Ellipse>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </Grid>

                                <!-- Xem chi tiết -->
                                <Grid Grid.Row="2">
                                    <Separator VerticalAlignment="Top" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                    <Button Content="Xem chi tiết" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF8062FE" Height="50" FontWeight="Normal" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnNghiPhepCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>

                        <!-- Thẻ nhên viên mới -->
                        <materialDesign:Card Width="280" Height="250" Margin="5,0,30,35">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"></RowDefinition>
                                    <RowDefinition Height="5*"></RowDefinition>
                                    <RowDefinition Height="2*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <!-- Tiêu đề thẻ -->
                                <Grid Grid.Row="0">
                                    <Rectangle VerticalAlignment="Top" Height="3" Fill="#ED9998"></Rectangle>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 10" >
                                        <TextBlock Text="Nhân viên mới" FontSize="18" Foreground="#FF404855" FontFamily="Segoe UI Semibold"></TextBlock>
                                        <Border  CornerRadius="8" Background="#ED9998" Width="20" Height="20" Margin="5,0,0,0">
                                            <TextBlock Text="{Binding SoLuongNhanVienMoi}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="12"></TextBlock>
                                        </Border>
                                    </StackPanel>
                                    <Separator VerticalAlignment="Bottom" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                </Grid>

                                <!-- Nội dung thẻ -->
                                <ScrollViewer  Grid.Row="1" Margin="10 0" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding ListNhanVienMoi}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Ellipse Width="35" Height="35" Margin="10" ToolTip="{Binding NhanVien.HOTEN_NV}">
                                                    <Ellipse.Fill>
                                                        <ImageBrush ImageSource="{Binding Avatar}" Stretch="Uniform" />
                                                    </Ellipse.Fill>
                                                </Ellipse>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <!-- Xem chi tiết -->
                                <Grid Grid.Row="2">
                                    <Separator VerticalAlignment="Top" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                    <Button Content="Xem chi tiết" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF8062FE" Height="50" FontWeight="Normal" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnNhanVienCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>

                        <!-- Thẻ sinh nhật trong tháng -->
                        <materialDesign:Card Width="280" Height="250" Margin="5,0,30,35">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"></RowDefinition>
                                    <RowDefinition Height="5*"></RowDefinition>
                                    <RowDefinition Height="2*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <!-- Tiêu đề thẻ -->
                                <Grid Grid.Row="0">
                                    <Rectangle VerticalAlignment="Top" Height="3" Fill="#E5BD62"></Rectangle>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 10" >
                                        <TextBlock Text="Sinh nhật trong tháng" FontSize="18" Foreground="#FF404855" FontFamily="Segoe UI Semibold"></TextBlock>
                                        <Border  CornerRadius="8" Background="#E5BD62" Width="20" Height="20" Margin="5,0,0,0">
                                            <TextBlock Text="{Binding SoLuongSinhNhatThang}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="12"></TextBlock>
                                        </Border>
                                    </StackPanel>
                                    <Separator VerticalAlignment="Bottom" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                </Grid>

                                <!-- Nội dung thẻ -->
                                <ListView Grid.Row="1" Margin="10 0" ItemsSource="{Binding ListNhanVienSinhNhatThang}">
                                    <ListView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical"/>
                                        </ItemsPanelTemplate>
                                    </ListView.ItemsPanel>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="35" Height="35" HorizontalAlignment="Left" ToolTip="{Binding NhanVien.HOTEN_NV}">
                                                    <Ellipse.Fill>
                                                        <ImageBrush ImageSource="{Binding Avatar}" Stretch="Uniform" />
                                                    </Ellipse.Fill>
                                                </Ellipse>
                                                <TextBlock Text="{Binding NgaySinh}" Margin="20 0 0 0" VerticalAlignment="Center" FontSize="16" Foreground="#FF6F7A80"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <!-- Xem chi tiết -->
                                <Grid Grid.Row="2">
                                    <Separator VerticalAlignment="Top" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                    <Button Content="Xem chi tiết" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF8062FE" Height="50" FontWeight="Normal" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnNhanVienCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>

                        <!-- Thẻ tuyển dụng mới-->
                        <materialDesign:Card Width="280" Height="250" Margin="5,0,30,35">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"></RowDefinition>
                                    <RowDefinition Height="5*"></RowDefinition>
                                    <RowDefinition Height="2*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <!-- Tiêu đề thẻ -->
                                <Grid Grid.Row="0">
                                    <Rectangle VerticalAlignment="Top" Height="3" Fill="#5AA1D6"></Rectangle>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 10" >
                                        <TextBlock Text="Tuyển dụng mới" FontSize="18" Foreground="#FF404855" FontFamily="Segoe UI Semibold"></TextBlock>

                                    </StackPanel>
                                    <Separator VerticalAlignment="Bottom" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                </Grid>

                                <!-- Nội dung thẻ -->
                                <Grid Grid.Row="1" Margin="10 0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition Height="4*"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <TextBlock Text="7 ngày qua" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF6F7A80" Margin="0"></TextBlock>
                                        </Grid>
                                        <Grid Grid.Row="1" Margin="0,5,0,0">
                                            <TextBlock Text="{Binding SoLuongTuyenDungTuan}" HorizontalAlignment="Center" FontSize="72" FontFamily="Segoe UI Light" />
                                        </Grid>
                                    </Grid>
                                    <Grid Grid.Column="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition Height="4*"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <TextBlock Text="Tháng này" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF6F7A80" Margin="0"></TextBlock>
                                        </Grid>
                                        <Grid Grid.Row="1" Margin="0,5,0,0">
                                            <TextBlock Text="{Binding SoLuongTuyenDungThang}" HorizontalAlignment="Center" FontSize="72" FontFamily="Segoe UI Light" />
                                        </Grid>
                                    </Grid>
                                </Grid>

                                <!-- Xem chi tiết -->
                                <Grid Grid.Row="2">
                                    <Separator VerticalAlignment="Top" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                    <Button Content="Xem chi tiết" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF8062FE" Height="50" FontWeight="Normal" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnTuyenDungCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>

                        <!-- Thẻ ngày lễ tiếp theo -->
                        <materialDesign:Card Width="280" Height="250" Margin="5,0,30,35">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"></RowDefinition>
                                    <RowDefinition Height="5*"></RowDefinition>
                                    <RowDefinition Height="2*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <!-- Tiêu đề thẻ -->
                                <Grid Grid.Row="0">
                                    <Rectangle VerticalAlignment="Top" Height="3" Fill="#AF8969"></Rectangle>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 10" >
                                        <TextBlock Text="Ngày lễ tiếp theo" FontSize="18" Foreground="#FF404855" FontFamily="Segoe UI Semibold"></TextBlock>

                                    </StackPanel>
                                    <Separator VerticalAlignment="Bottom" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                </Grid>

                                <!-- Nội dung thẻ -->
                                <Grid Grid.Row="1" Margin="10 0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2">
                                        <TextBlock Margin="0 20" Text="Không có ngày nghỉ lễ kế tiếp" HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HienNgayLe}" Value="false">
                                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <Grid Grid.Column="0" Visibility="{Binding Path=NgayNghiKeTiep1, TargetNullValue=Collapsed}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="2*"></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0" Height="60" Width="60">
                                            <Grid.RowDefinitions>
                                                <RowDefinition></RowDefinition>
                                                <RowDefinition Height="3*"></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <Border Grid.Row="0" CornerRadius="20 20 0 0" Background="#D83556">
                                                <Grid>
                                                    <TextBlock Text="{Binding NgayNghiKeTiep1.Thang}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="9" Foreground="White"></TextBlock>
                                                </Grid>
                                            </Border>
                                            <Border Grid.Row="1" CornerRadius="0 0 20 20" Background="#F2F4F5">
                                                <Grid>
                                                    <TextBlock Text="{Binding NgayNghiKeTiep1.Ngay}" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="24"></TextBlock>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                        <Grid Grid.Row="1">
                                            <TextBlock Text="{Binding NgayNghiKeTiep1.Ten}" VerticalAlignment="Top" HorizontalAlignment="Center" TextWrapping="Wrap" Foreground="#FF6F7A80" FontSize="14"/>
                                        </Grid>
                                    </Grid>
                                    <Grid Grid.Column="1" Visibility="{Binding Path=NgayNghiKeTiep2, TargetNullValue=Collapsed}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="2*"></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0" Height="60" Width="60">
                                            <Grid.RowDefinitions>
                                                <RowDefinition></RowDefinition>
                                                <RowDefinition Height="3*"></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <Border Grid.Row="0" CornerRadius="20 20 0 0" Background="#D83556">
                                                <Grid>
                                                    <TextBlock Text="{Binding NgayNghiKeTiep2.Thang}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="9" Foreground="White"></TextBlock>
                                                </Grid>
                                            </Border>
                                            <Border Grid.Row="1" CornerRadius="0 0 20 20" Background="#F2F4F5">
                                                <Grid>
                                                    <TextBlock Text="{Binding NgayNghiKeTiep2.Ngay}" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="24"></TextBlock>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                        <Grid Grid.Row="1">
                                            <TextBlock Text="{Binding NgayNghiKeTiep2.Ten}" VerticalAlignment="Top" HorizontalAlignment="Center" TextWrapping="Wrap" Foreground="#FF6F7A80" FontSize="14"/>
                                        </Grid>
                                    </Grid>
                                </Grid>

                                <!-- Xem chi tiết -->
                                <Grid Grid.Row="2">
                                    <Separator VerticalAlignment="Top" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                    <Button Content="Xem chi tiết" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF8062FE" Height="50" FontWeight="Normal" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnCaiDatCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>

                        <!-- Thẻ thông báo -->
                        <materialDesign:Card Width="280" Height="250" Margin="5,0,30,35">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"></RowDefinition>
                                    <RowDefinition Height="5*"></RowDefinition>
                                    <RowDefinition Height="2*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <!-- Tiêu đề thẻ -->
                                <Grid Grid.Row="0">
                                    <Rectangle VerticalAlignment="Top" Height="3" Fill="#989CA0"></Rectangle>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 10" >
                                        <TextBlock Text="Thông báo" FontSize="18" Foreground="#FF404855" FontFamily="Segoe UI Semibold"></TextBlock>

                                    </StackPanel>
                                    <Separator VerticalAlignment="Bottom" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                </Grid>

                                <!-- Nội dung thẻ -->
                                <TextBlock Grid.Row="1" Foreground="#FF6F7A80" Margin="20 10" TextWrapping="Wrap" Text="{Binding ThongBao}" VerticalAlignment="Top" FontSize="14"/>

                                <!-- Xem chi tiết -->
                                <Grid Grid.Row="2">
                                    <Separator VerticalAlignment="Top" Style="{StaticResource MaterialDesignDarkSeparator}"/>
                                    <Button Content="Xoá thông báo" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF8062FE" Height="50" FontWeight="Normal" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding XoaThongBaoCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </Button>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>
                    </WrapPanel>
                </ScrollViewer>
            </Grid>

            <!-- Nhân viên -->
            <Grid Name="gridNhanVien">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="1">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Nhân viên" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource NhanVienVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Thanh chức năng -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchNhanVien, UpdateSourceTrigger=PropertyChanged}">

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                            <Grid Width="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"></ColumnDefinition>
                                    <ColumnDefinition Width="3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                </Grid>
                                <Grid Grid.Column="1">
                                    <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                </Grid>
                            </Grid>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>

                    <!-- Danh sách nhân viên -->
                    <Grid Grid.Row="1">
                        <ListView Name="listNhanVien"
                                  ItemsSource="{Binding ListNhanVien, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                  SelectedItem="{Binding SelectedNhanVien, Mode=OneWayToSource}" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_NV}" >
                                        <GridViewColumnHeader Tag="MA_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="100">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding HOTEN_NV}">
                                        <GridViewColumnHeader Tag="HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Họ tên" Width="250">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding CHUCVU_NV}">
                                        <GridViewColumnHeader Tag="CHUCVU_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Chức vụ" Width="300">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYVAOLAM_NV, StringFormat='dd/MM/yyyy'}">
                                        <GridViewColumnHeader Tag="NGAYVAOLAM_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày vào làm" Width="150">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding SODIENTHOAI_NV}">
                                        <GridViewColumnHeader Tag="SODIENTHOAI_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Số điện thoại" Width="150">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Phòng ban -->
            <Grid Name="gridPhongBan">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="2">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Phòng ban" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->

                <Grid Grid.Row="1" DataContext="{StaticResource PhongBanVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Thanh chức năng -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchPhongBan, UpdateSourceTrigger=PropertyChanged}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                            <Grid Width="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"></ColumnDefinition>
                                    <ColumnDefinition Width="3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                </Grid>
                                <Grid Grid.Column="1">
                                    <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                </Grid>
                            </Grid>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>

                    <!-- Danh sách phòng ban -->
                    <Grid Grid.Row="1">
                        <ListView Name="listPhongBan"
                                  ItemsSource="{Binding ListPhongBan}" 
                                  SelectedItem="{Binding SelectedPhongBan, Mode=OneWayToSource}" >

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_PB}" >
                                        <GridViewColumnHeader Tag="MA_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="100">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding TEN_PB}">
                                        <GridViewColumnHeader Tag="TEN_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên phòng ban" Width="250">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYTHANHLAP_PB, StringFormat='dd/MM/yyyy'}">
                                        <GridViewColumnHeader Tag="NGAYTHANHLAP_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày thành lập" Width="150">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding DIACHI_PB}">
                                        <GridViewColumnHeader Tag="DIACHI_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Địa chỉ" Width="250">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Nghỉ phép -->
            <Grid Name="gridNghiPhep">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="3">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Nghỉ phép" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource NghiPhepVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Loaded">
                            <i:InvokeCommandAction Command="{Binding LoadDataNPCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>

                    <!-- Tiêu đề các tab -->
                    <Grid Grid.Row="0">
                        <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                            <!-- Ngày nghỉ phép -->
                            <StackPanel>
                                <Button Name="btnNgayNghiPhep" Content="Ngày nghỉ phép" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangNP}" Value="0">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabNgayNghiPhepCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangNP}" Value="0">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>

                            <!-- Khoản nghỉ phép -->
                            <StackPanel>
                                <Button Name="btnKhoanNghiPhep" Content="Khoản nghỉ phép" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangNP}" Value="1">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabKhoanNghiPhepCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangNP}" Value="1">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>

                            <!-- Loại nghỉ phép -->
                            <StackPanel>
                                <Button Name="btnLoaiNghiPhep" Content="Loại nghỉ phép" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangNP}" Value="2">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabLoaiNghiPhepCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangNP}" Value="2">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>
                        </StackPanel>
                        <GridSplitter Height="1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" IsEnabled="False" Background="#FFD6D6D6"></GridSplitter>
                    </Grid>

                    <!-- Nội dung tab -->
                    <Grid Grid.Row="1">
                        <!-- Tab Ngày nghỉ phép -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangNP}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchNghiPhep, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <ListView Grid.Row="1" Name="listNghiPhep" 
                                  ItemsSource="{Binding ListNghiPhep}" 
                                  SelectedItem="{Binding SelectedNghiPhep, Mode=OneWayToSource}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn DisplayMemberBinding="{Binding MA_NV}" >
                                            <GridViewColumnHeader Tag="MA_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã nhân viên" Width="150">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}">
                                            <GridViewColumnHeader Tag="NHANVIEN.HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="350">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding NGAYBATDAU_NP, StringFormat='dd/MM/yyyy'}">
                                            <GridViewColumnHeader Tag="NGAYBATDAU_NP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày bắt đầu" Width="200">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding NGAYKETTHUC_NP, StringFormat='dd/MM/yyyy'}">
                                            <GridViewColumnHeader Tag="NGAYKETTHUC_NP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày kết thúc" Width="200">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>

                        <!-- Tab Khoản nghỉ phép -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangNP}" Value="1">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20" DataContext="{StaticResource KhoanNghiPhepVM}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding LoadDataKNPCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchKhoanNghiPhep, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <ListView Grid.Row="1" Name="listKhoanNghiPhep" DataContext="{StaticResource KhoanNghiPhepVM}"
                                  ItemsSource="{Binding ListKhoanNghiPhep_MainWD}"
                                  SelectedItem="{Binding SelectedKhoanNghiPhep, Mode=OneWayToSource}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}">
                                            <GridViewColumnHeader Tag="NHANVIEN.HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Họ tên nhân viên" Width="250">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding LOAINGHIPHEP.TEN_LNP}">
                                            <GridViewColumnHeader Tag="LOAINGHIPHEP.TEN_LNP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Loại nghỉ phép" Width="300">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding SONGAYNGHI_KNP}">
                                            <GridViewColumnHeader Tag="SONGAYNGHI_KNP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Số ngày nghỉ" Width="300">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>

                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.ContainerStyle>
                                            <Style TargetType="{x:Type GroupItem}">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <Expander Background="White" IsExpanded="False">
                                                                <Expander.Header>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock Text="{Binding Name}" FontSize="14" FontFamily="Segoe UI Semibold"/>
                                                                        <TextBlock Text="{Binding ItemCount}" FontSize="14" Margin="10,0,0,0"/>
                                                                        <TextBlock Text=" chi tiết" FontSize="14" />
                                                                    </StackPanel>
                                                                </Expander.Header>
                                                                <ItemsPresenter />
                                                            </Expander>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.ContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>

                            </ListView>
                        </Grid>

                        <!-- Tab Loại nghỉ phép -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangNP}" Value="2">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20" DataContext="{StaticResource LoaiNghiPhepVM}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchLoaiNghiPhep, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <ListView Grid.Row="1" Name="listLoaiNghiPhep" DataContext="{StaticResource LoaiNghiPhepVM}"
                                  ItemsSource="{Binding ListLoaiNghiPhep}" 
                                  SelectedItem="{Binding SelectedLoaiNghiPhep, Mode=OneWayToSource}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn DisplayMemberBinding="{Binding MA_LNP}" >
                                            <GridViewColumnHeader Tag="MA_LNP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="100">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding TEN_LNP}">
                                            <GridViewColumnHeader Tag="TEN_LNP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên loại nghỉ phép" Width="250">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn>
                                            <GridViewColumnHeader Tag="COLUONG_LNP" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Có lương?" Width="300">
                                            </GridViewColumnHeader>
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Name="CoLuongText" Text="{Binding Path=COLUONG_LNP, TargetNullValue='No Title Found'}"/>
                                                    <DataTemplate.Triggers>
                                                        <DataTrigger Binding="{Binding COLUONG_LNP}" Value="True">
                                                            <Setter TargetName="CoLuongText" Property="Text" Value="Có" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding COLUONG_LNP}" Value="False">
                                                            <Setter TargetName="CoLuongText" Property="Text" Value="Không" />
                                                        </DataTrigger>
                                                    </DataTemplate.Triggers>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                        </Grid>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Chấm công -->
            <Grid Name="gridChamCong">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="4">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Chấm công" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource ChamCongVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Loaded">
                            <i:InvokeCommandAction Command="{Binding LoadDataCCCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>

                    <!-- Thanh chức năng -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchNhanVien, UpdateSourceTrigger=PropertyChanged}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                            <Grid Width="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"></ColumnDefinition>
                                    <ColumnDefinition Width="3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                </Grid>
                                <Grid Grid.Column="1">
                                    <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                </Grid>
                            </Grid>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>

                    <!-- Danh sách nhân viên -->
                    <Grid Grid.Row="1">
                        <ListView DataContext="{StaticResource ChamCongVM}"
                                  ItemsSource="{Binding ListNhanVien}" 
                                  SelectedItem="{Binding SelectedNhanVien, Mode=OneWayToSource}" >

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_NV}" >
                                        <GridViewColumnHeader Tag="MA_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã nhân viên" Width="150">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding HOTEN_NV}">
                                        <GridViewColumnHeader Tag="HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Họ tên" Width="250">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding PHONGBAN.TEN_PB}">
                                        <GridViewColumnHeader Tag="PHONGBAN.TEN_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding CHUCVU_NV}">
                                        <GridViewColumnHeader Tag="CHUCVU_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Chức vụ" Width="300">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Lương -->
            <Grid Name="gridLuong">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="5">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Lương" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource LuongVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Tiêu đề các tab -->
                    <Grid Grid.Row="0">
                        <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                            <!-- Bảng lương -->
                            <StackPanel>
                                <Button Content="Bảng lương" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangBL}" Value="0">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabBangLuongCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangBL}" Value="0">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>

                            <!-- Khoản lương -->
                            <StackPanel>
                                <Button Content="Khoản lương" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangBL}" Value="1">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabKhoanLuongCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangBL}" Value="1">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>

                            <!-- Loại lương -->
                            <StackPanel>
                                <Button Content="Loại lương" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangBL}" Value="2">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabLoaiLuongCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangBL}" Value="2">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>
                        </StackPanel>
                        <GridSplitter Height="1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" IsEnabled="False" Background="#FFD6D6D6"></GridSplitter>
                    </Grid>

                    <!-- Nội dung tab -->
                    <Grid Grid.Row="1">
                        <!-- Tab Bảng lương -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangBL}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <StackPanel Width="125" VerticalAlignment="Center" Margin="0 0 50 0">
                                    <TextBlock Foreground="#6F7A80" 
                                       Text="Chọn năm"></TextBlock>
                                    <ComboBox ItemsSource="{Binding ListNam}" SelectedItem="{Binding SelectedNam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="SelectionChanged">
                                                <i:InvokeCommandAction Command="{Binding ChangeCmbCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Width="125" VerticalAlignment="Center" Margin="0 0 50 0">
                                    <TextBlock Foreground="#6F7A80" 
                                       Text="Chọn tháng"></TextBlock>
                                    <ComboBox ItemsSource="{Binding ListThang}" SelectedItem="{Binding SelectedThang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="SelectionChanged">
                                                <i:InvokeCommandAction Command="{Binding ChangeCmbCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Width="250" VerticalAlignment="Center" Margin="0 0 50 0">
                                    <TextBlock Foreground="#6F7A80" 
                                       Text="Chọn phòng ban"></TextBlock>
                                    <ComboBox ItemsSource="{Binding ListPhongBan}" SelectedItem="{Binding SelectedPhongBan, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0" DisplayMemberPath="TEN_PB">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="SelectionChanged">
                                                <i:InvokeCommandAction Command="{Binding ChangeCmbCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>

                            <Button Grid.Row="0" Height="40" HorizontalAlignment="Right" VerticalAlignment="Center" 
                                        BorderBrush="{x:Null}" Background="#8062FE"
                                    Content="Tính lương" FontWeight="Normal" FontSize="16">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding TinhLuongCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Button>

                            <!-- Nội dung -->
                            <ListView Grid.Row="2"
                                  ItemsSource="{Binding ListBangLuong}" 
                                  SelectedItem="{Binding SelectedBangLuong, Mode=OneWayToSource}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.MA_NV}" >
                                            <GridViewColumnHeader Tag="NHANVIEN.MA_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã nhân viên" Width="150">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}">
                                            <GridViewColumnHeader Tag="NHANVIEN.HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="250">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.CHUCVU_NV}">
                                            <GridViewColumnHeader Tag="NHANVIEN.CHUCVU_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Chức vụ" Width="200">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.PHONGBAN.TEN_PB}">
                                            <GridViewColumnHeader Tag="NHANVIEN.PHONGBAN.TEN_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="200">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding TONGLUONG_BL, StringFormat='N0'}">
                                            <GridViewColumnHeader Tag="TONGLUONG_BL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tổng lương" Width="200">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                        </Grid>

                        <!-- Tab Khoản lương -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangBL}" Value="1">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20" DataContext="{StaticResource KhoanLuongVM}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchKhoanLuong, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <ListView Grid.Row="1" DataContext="{StaticResource KhoanLuongVM}"
                                  ItemsSource="{Binding ListKhoanLuong_MainWD}" 
                                  SelectedItem="{Binding SelectedKhoanLuong, Mode=OneWayToSource}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}">
                                            <GridViewColumnHeader Tag="NHANVIEN.HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Họ tên nhân viên" Width="250">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding LOAILUONG.TEN_LL}">
                                            <GridViewColumnHeader Tag="LOAILUONG.TEN_LL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Loại lương" Width="300">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding SOTIEN_KL, StringFormat='N0'}">
                                            <GridViewColumnHeader Tag="SOTIEN_KL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Số tiền" Width="300">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>

                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.ContainerStyle>
                                            <Style TargetType="{x:Type GroupItem}">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <Expander Background="White" IsExpanded="False">
                                                                <Expander.Header>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock Text="{Binding Name}" FontSize="14" FontFamily="Segoe UI Semibold"/>
                                                                        <TextBlock Text="{Binding ItemCount}" FontSize="14" Margin="10,0,0,0"/>
                                                                        <TextBlock Text=" chi tiết" FontSize="14" />
                                                                    </StackPanel>
                                                                </Expander.Header>
                                                                <ItemsPresenter />
                                                            </Expander>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.ContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>

                            </ListView>
                        </Grid>

                        <!-- Tab Loại lương -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangBL}" Value="2">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20" DataContext="{StaticResource LoaiLuongVM}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchLoaiLuong, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <ListView Grid.Row="1" DataContext="{StaticResource LoaiLuongVM}"
                                  ItemsSource="{Binding ListLoaiLuong}" 
                                  SelectedItem="{Binding SelectedLoaiLuong, Mode=OneWayToSource}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn DisplayMemberBinding="{Binding MA_LL}" >
                                            <GridViewColumnHeader Tag="MA_LL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="100">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                        <GridViewColumn DisplayMemberBinding="{Binding TEN_LL}">
                                            <GridViewColumnHeader Tag="TEN_LL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên loại lương" Width="250">
                                            </GridViewColumnHeader>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                        </Grid>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Tuyển dụng -->
            <Grid Name="gridTuyenDung">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="6">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Tuyển dụng" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource TuyenDungVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Thanh chức năng -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchUngVien, UpdateSourceTrigger=PropertyChanged}">

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                            <Grid Width="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"></ColumnDefinition>
                                    <ColumnDefinition Width="3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                </Grid>
                                <Grid Grid.Column="1">
                                    <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                </Grid>
                            </Grid>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>

                    <!-- Danh sách ứng viên -->
                    <Grid Grid.Row="1">
                        <ListView Name="listUngVien"
                                  ItemsSource="{Binding ListUngVien}" 
                                  SelectedItem="{Binding SelectedUngVien, Mode=OneWayToSource}" >

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_UV}" >
                                        <GridViewColumnHeader Tag="MA_UV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="80">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding HOTEN_UV}" >
                                        <GridViewColumnHeader Tag="HOTEN_UV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Họ tên" Width="220">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYSINH_UV, StringFormat=dd/MM/yyyy}">
                                        <GridViewColumnHeader Tag="NGAYSINH_UV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày sinh" Width="100">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding EMAIL_UV}">
                                        <GridViewColumnHeader Tag="EMAIL_UV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Email" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Chi phí -->
            <Grid Name="gridChiPhi">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="7">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Chi phí" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource ChiPhiVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Thanh chức năng -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchPhieuChi, UpdateSourceTrigger=PropertyChanged}">

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                            <Grid Width="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"></ColumnDefinition>
                                    <ColumnDefinition Width="3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                </Grid>
                                <Grid Grid.Column="1">
                                    <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                </Grid>
                            </Grid>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>

                    <!-- Danh sách Phiếu chi -->
                    <Grid Grid.Row="1">
                        <ListView Name="listChiPhi"
                                  ItemsSource="{Binding ListPhieuChi}" 
                                  SelectedItem="{Binding SelectedPhieuChi, Mode=OneWayToSource}" >

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_PC}" >
                                        <GridViewColumnHeader Tag="MA_PC" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="80">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}" >
                                        <GridViewColumnHeader Tag="HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="220">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding TRIGIA_PC,  StringFormat='N0'}">
                                        <GridViewColumnHeader Tag="TRIGIA_PC" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Trị giá" Width="100">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding THOIGIANLAP_PC, StringFormat=dd/MM/yyyy}">
                                        <GridViewColumnHeader Tag="THOIGIANLAP_PC" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Thời gian lập" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Báo cáo -->
            <Grid Name="gridBaoCao">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="8">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Báo cáo" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource BaoCaoVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Chọn loại báo cáo -->
                    <Grid Grid.Row="0">
                        <StackPanel Width="300" Margin="0 0 25 0" HorizontalAlignment="Left">
                            <TextBlock Foreground="#6F7A80" 
                                       Text="Loại báo cáo"></TextBlock>
                            <ComboBox ItemsSource="{Binding ListChucNangBaoCao}" SelectedItem="{Binding SelectedChucNangBaoCao, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding ChangedChucNangBaoCaoCommand}">
                                        </i:InvokeCommandAction>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Báo cáo nhân viên -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Trạng thái nhân viên"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListTrangThaiNhanVien}" SelectedItem="{Binding SelectedTrangThaiNhanVien, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedTrangThaiNhanVienCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>


                        <!-- Danh sách nhân viên -->
                        <ListView Grid.Row="1"
                            Name="listNhanVien_BaoCao"
                                  ItemsSource="{Binding ListNhanVien}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_NV}" >
                                        <GridViewColumnHeader Tag="MA_NV" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã Nhân viên" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding HOTEN_NV}" >
                                        <GridViewColumnHeader Tag="HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding CHUCVU_NV}">
                                        <GridViewColumnHeader Tag="CHUCVU_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Chức vụ" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYVAOLAM_NV,StringFormat=dd/MM/yyyy}">
                                        <GridViewColumnHeader Tag="NGAYVAOLAM_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày vào làm" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding SODIENTHOAI_NV}">
                                        <GridViewColumnHeader Tag="SODIENTHOAI_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Số điện thoại" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding PHONGBAN.TEN_PB}">
                                        <GridViewColumnHeader Tag="THOIGIANLAP_PC"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>

                    <!-- Báo cáo phòng ban -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="1">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">

                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>


                        <!-- Danh sách phòng ban -->
                        <ListView Grid.Row="1"
                            Name="listPhongBan_BaoCao"
                                  ItemsSource="{Binding ListPhongBan}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_PB}" >
                                        <GridViewColumnHeader Tag="MA_PB" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã Phòng ban" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding TEN_PB}" >
                                        <GridViewColumnHeader Tag="TEN_PB"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên phòng ban" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYTHANHLAP_PB,StringFormat='dd/MM/yyyy'}">
                                        <GridViewColumnHeader Tag="NGAYTHANHLAP_PB"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày thành lập" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding DIACHI_PB}">
                                        <GridViewColumnHeader Tag="DIACHI_PB"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 

                                                              VerticalAlignment="Center" Content="Địa chỉ" Width="250">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>

                    <!-- Báo cáo nghỉ phép -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="2">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Tháng"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListThang}" SelectedItem="{Binding SelectedThang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedThangCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel  Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Năm"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListNam}" SelectedItem="{Binding SelectedNam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedNamCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>


                        <!-- Danh sách nghỉ phép -->
                        <ListView Grid.Row="1"
                            Name="listNghiPhep_BaoCao"
                                  ItemsSource="{Binding ListNghiPhep}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_NP}" >
                                        <GridViewColumnHeader Tag="MA_NP" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã Nghỉ phép" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}" >
                                        <GridViewColumnHeader Tag="HOTEN_NV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.PHONGBAN.TEN_PB}">
                                        <GridViewColumnHeader Tag="TEN_PB"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding KHOANNGHIPHEP.LOAINGHIPHEP.TEN_LNP}">
                                        <GridViewColumnHeader Tag="TEN_KNP"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên loại nghỉ phép" Width="140">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYBATDAU_NP,StringFormat=dd/MM/yyyy}">
                                        <GridViewColumnHeader Tag="NGAYBATDAU_NP"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày bắt đầu" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYKETTHUC_NP,StringFormat=dd/MM/yyyy}">
                                        <GridViewColumnHeader Tag="NGAYKETTHUC_NP"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày kết thúc" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding LIDO_NP}">
                                        <GridViewColumnHeader Tag="LIDO_NP"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Lí do" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>

                    </Grid>

                    <!-- Báo cáo chấm công -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="3">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Tháng"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListThang}" SelectedItem="{Binding SelectedThang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedThangCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel  Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Năm"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListNam}" SelectedItem="{Binding SelectedNam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedNamCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>


                        <!-- Danh sách chấm công -->
                        <ListView Grid.Row="1"
                            Name="listChamCong_BaoCao"
                                  ItemsSource="{Binding ListChamCong}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_CCN}" >
                                        <GridViewColumnHeader Tag="MA_PB" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã chấm công" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}" >
                                        <GridViewColumnHeader Tag="HOTEN_NV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding LOAICHAMCONG.TEN_LCC}" >
                                        <GridViewColumnHeader Tag="TEN_LCC"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên loại chấm công" Width="150">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding THOIGIANBATDAU_CCN,StringFormat='dd/MM/yyyy HH:mm'}">
                                        <GridViewColumnHeader Tag="THOIGIANBATDAU_CCN"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Thời gian bắt đầu" Width="180">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding THOIGIANKETTHUC_CCN, StringFormat='dd/MM/yyyy HH:mm'}">
                                        <GridViewColumnHeader Tag="THOIGIANKETTHUC_CCN"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Thời gian kết thúc" Width="180">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.PHONGBAN.TEN_PB}">
                                        <GridViewColumnHeader Tag="THOIGIANKETTHUC_CCN"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>


                                </GridView>
                            </ListView.View>
                        </ListView>

                    </Grid>

                    <!-- Báo cáo Lương -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="4">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Tháng"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListThang}" SelectedItem="{Binding SelectedThang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedThangCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel  Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Năm"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListNam}" SelectedItem="{Binding SelectedNam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedNamCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>


                        <!-- Danh sách lương -->
                        <ListView Grid.Row="1"
                            Name="listLuong_BaoCao"
                                  ItemsSource="{Binding ListBangLuong}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_BL}" >
                                        <GridViewColumnHeader Tag="MA_BL" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã lương" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}" >
                                        <GridViewColumnHeader Tag="HOTEN_NV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.CHUCVU_NV}" >
                                        <GridViewColumnHeader Tag="CHUCVU_NV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Chức vụ" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding TONGLUONG_BL,StringFormat='N0'}" >
                                        <GridViewColumnHeader Tag="TONGLUONG_BL"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tổng lương" Width="250">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.PHONGBAN.TEN_PB}" >
                                        <GridViewColumnHeader Tag="TEN_PB"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>



                                </GridView>
                            </ListView.View>
                        </ListView>

                    </Grid>

                    <!-- Báo cáo Ứng viên -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="5">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">

                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>


                        <!-- Danh sách ứng viên -->
                        <ListView Grid.Row="1"
                            Name="listUngVien_BaoCao"
                                  ItemsSource="{Binding ListUngVien}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_UV}" >
                                        <GridViewColumnHeader Tag="MA_UV" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã ứng viên" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding HOTEN_UV}" >
                                        <GridViewColumnHeader Tag="HOTEN_UV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên ứng viên" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding EMAIL_UV}" >
                                        <GridViewColumnHeader Tag="EMAIL_UV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Email" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding SODIENTHOAI_UV}" >
                                        <GridViewColumnHeader Tag="SODIENTHOAI_UV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Số điện thoại" Width="130">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding DIACHI_UV}" >
                                        <GridViewColumnHeader Tag="DIACHI_UV"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Địa chỉ" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>


                                </GridView>
                            </ListView.View>
                        </ListView>

                    </Grid>

                    <!-- Báo cáo Chi phí  -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="6">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Tháng"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListThang}" SelectedItem="{Binding SelectedThang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedThangCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel  Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Năm"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListNam}" SelectedItem="{Binding SelectedNam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedNamCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <!-- Danh sách phiếu chi -->
                        <ListView Grid.Row="1"
                            Name="listPhieuChi_BaoCao"
                                  ItemsSource="{Binding ListPhieuChi}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_PC}" >
                                        <GridViewColumnHeader Tag="MA_PC" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã phiếu chi" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}" >
                                        <GridViewColumnHeader Tag="HOTEN_NV" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding TRIGIA_PC,StringFormat='N0'}" >
                                        <GridViewColumnHeader Tag="TRIGIA_PC"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Trị giá" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding THOIGIANLAP_PC,StringFormat='dd/MM/yyyy HH:mm'}" >
                                        <GridViewColumnHeader Tag="THOIGIANLAP_PC"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Thời gian lập" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.PHONGBAN.TEN_PB}" >
                                        <GridViewColumnHeader Tag="TEN_PB" 
                                                              
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Phòng ban" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>
                        </ListView>

                    </Grid>

                    <!-- Báo cáo Ngày nghỉ lễ  -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="343*"/>
                            <ColumnDefinition Width="660*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangBC}" Value="7">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>

                        <!-- Thanh chức năng-->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
                            <StackPanel Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Tháng"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListThang}" SelectedItem="{Binding SelectedThang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedThangCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel  Width="125" Margin="0 0 25 0">
                                <TextBlock Foreground="#6F7A80" 
                                       Text="Năm"></TextBlock>
                                <ComboBox ItemsSource="{Binding ListNam}" SelectedItem="{Binding SelectedNam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      FontSize="16" Margin="0,5,0,0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding ChangedNamCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Height="40" Content="Xuất báo cáo" VerticalAlignment="Center" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE" FontWeight="Normal" Margin="0,15">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding XuatBaoCaoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <!-- Danh sách ngày nghỉ lễ -->
                        <ListView Grid.Row="1"
                            Name="listNgayNghiLe_BaoCao"
                                  ItemsSource="{Binding ListNgayNghiLe}" Grid.ColumnSpan="2" 
                                  >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn DisplayMemberBinding="{Binding MA_NNL}" >
                                        <GridViewColumnHeader Tag="MA_NNL" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Mã ngày nghỉ" Width="120">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>

                                    <GridViewColumn DisplayMemberBinding="{Binding NGAYNGHILE_NNL,StringFormat='dd/MM/yyyy'}" >
                                        <GridViewColumnHeader Tag="NGAYNGHILE_NNL"                                                               

                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding TEN_NNL}" >
                                        <GridViewColumnHeader Tag="TEN_NNL"                                                               
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên ngày nghỉ lễ" Width="200">
                                        </GridViewColumnHeader>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>

                    </Grid>

                </Grid>
            </Grid>

            <!-- Cài đặt -->
            <Grid Name="gridCaiDat">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChucNangNS}" Value="9">
                                <Setter Property="Visibility" Value="Visible"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="130"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <!-- Tiêu đề trang -->
                <Grid Grid.Row="0" Margin="0 0 -20 10">
                    <Grid.Background>
                        <ImageBrush ImageSource="Resources/Icons/background.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                    <TextBlock Text="Cài đặt" FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                </Grid>

                <!-- Nội dung trang -->
                <Grid Grid.Row="1" DataContext="{StaticResource TaiKhoanVM}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- Tiêu đề các tab -->
                    <Grid Grid.Row="0">
                        <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                            <!-- Tài khoản -->
                            <StackPanel>
                                <Button Content="Tài khoản" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangCD}" Value="0">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabTaiKhoanCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangCD}" Value="0">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>

                            <!-- Ngày nghỉ lễ -->
                            <StackPanel>
                                <Button Content="Ngày nghỉ lễ" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangCD}" Value="1">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabNgayNghiLeCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangCD}" Value="1">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>

                            <!-- Thông tin phần mềm -->
                            <StackPanel>
                                <Button Content="Thông tin phần mềm" Height="35" BorderBrush="{x:Null}" 
                                    Background="{x:Null}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Setter Property="FontWeight" Value="Normal"></Setter>
                                            <Setter Property="Foreground" Value="#6F7A80"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangCD}" Value="2">
                                                    <Setter Property="FontWeight" Value="DemiBold"></Setter>
                                                    <Setter Property="Foreground" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TabThongTinPhanMemCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Rectangle Height="3" Margin="0">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Fill" Value="{x:Null}"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangCD}" Value="2">
                                                    <Setter Property="Fill" Value="#8062FE"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </StackPanel>
                        </StackPanel>
                        <GridSplitter Height="1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" IsEnabled="False" Background="#FFD6D6D6"></GridSplitter>
                    </Grid>

                    <!-- Nội dung các tab -->
                    <Grid Grid.Row="1">
                        <!-- Tab Tài khoản -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangCD}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchTaiKhoan, UpdateSourceTrigger=PropertyChanged}">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <Grid Grid.Row="1">
                                <ListView Name="listTaiKhoan"
                                  ItemsSource="{Binding ListTaiKhoan}" 
                                  SelectedItem="{Binding SelectedTaiKhoan, Mode=OneWayToSource}" >

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="SelectionChanged">
                                            <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn DisplayMemberBinding="{Binding TENDANGNHAP_TK}" >
                                                <GridViewColumnHeader Tag="TENDANGNHAP_TK" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên tài khoản" Width="200">
                                                </GridViewColumnHeader>
                                            </GridViewColumn>
                                            <GridViewColumn DisplayMemberBinding="{Binding QUYEN_TK}" >
                                                <GridViewColumnHeader Tag="QUYEN_TK" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Quyền hạn" Width="300">
                                                </GridViewColumnHeader>
                                            </GridViewColumn>
                                            <GridViewColumn DisplayMemberBinding="{Binding NHANVIEN.HOTEN_NV}" >
                                                <GridViewColumnHeader Tag="HOTEN_NV" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên nhân viên" Width="300">
                                                </GridViewColumnHeader>
                                            </GridViewColumn>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </Grid>
                        </Grid>

                        <!-- Tab Ngày nghỉ lễ -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangCD}" Value="1">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <!-- Thanh chức năng-->
                            <Grid Grid.Row="0" Margin="0,0,0,20" DataContext="{StaticResource NgayNghiLeVM}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Tìm kiếm"
                                 Text="{Binding SearchNgayNghiLe, UpdateSourceTrigger=PropertyChanged}">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction Command="{Binding SearchCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" BorderBrush="{x:Null}" Background="#8062FE">
                                    <Grid Width="100">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"></ColumnDefinition>
                                            <ColumnDefinition Width="3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Image Stretch="None" Source="Resources\Icons\btnThem_white.png"></Image>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <TextBlock Text="Tạo mới" HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       FontSize="16" Margin="5,0,0,0" FontWeight="Normal" ></TextBlock>
                                        </Grid>
                                    </Grid>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding TaoMoiCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>

                            <!-- Nội dung -->
                            <Grid Grid.Row="1" DataContext="{StaticResource NgayNghiLeVM}">
                                <Grid Grid.Row="1">
                                    <ListView Name="listNgayNghiLe"
                                  ItemsSource="{Binding ListNgayNghiLe}" 
                                  SelectedItem="{Binding SelectedNgayNghiLe, Mode=OneWayToSource}" >

                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="SelectionChanged">
                                                <i:InvokeCommandAction Command="{Binding HienThiCommand}">
                                                </i:InvokeCommandAction>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>

                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn DisplayMemberBinding="{Binding MA_NNL}" >
                                                    <GridViewColumnHeader Tag="MA_NNL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="ID" Width="100">
                                                    </GridViewColumnHeader>
                                                </GridViewColumn>
                                                <GridViewColumn DisplayMemberBinding="{Binding TEN_NNL}">
                                                    <GridViewColumnHeader Tag="TEN_PB" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Tên ngày nghỉ lễ" Width="250">
                                                    </GridViewColumnHeader>
                                                </GridViewColumn>
                                                <GridViewColumn DisplayMemberBinding="{Binding NGAY_NNL, StringFormat=dd/MM/yyyy}">
                                                    <GridViewColumnHeader Tag="NGAY_NNL" 
                                                              Command="{Binding SortCommand}" 
                                                              CommandParameter="{Binding RelativeSource={RelativeSource Self}}" 
                                                              VerticalAlignment="Center" Content="Ngày" Width="150">
                                                    </GridViewColumnHeader>
                                                </GridViewColumn>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                            </Grid>
                        </Grid>

                        <!-- Tab Thông tin phần mềm -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNangCD}" Value="2">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <TextBlock>Thông tin phần mềm</TextBlock>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>

        </Grid>
    </Grid>
</Window>